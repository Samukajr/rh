# Servidor
PORT=3000
NODE_ENV=development

# Banco de Dados PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rhplus
DB_USER=rhplus
DB_PASS=rhplus123

# Redis
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=desenvolvimento_jwt_secret_super_seguro_12345
JWT_EXPIRES_IN=7d

# AWS S3 (opcional - para upload de arquivos)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_BUCKET_NAME=
AWS_REGION=us-east-1

# Email SMTP (opcional - para notificações)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@rhplus.com

# Upload local (fallback se não usar S3)
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760

# Rate Limiting
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100

# CORS
CORS_ORIGIN=http://localhost:5173